.row
  .span12
    %section.login
      %header.page-heade
        / %h1 インシュランスとしてログイン

      - flash.each do |key, value|
        = content_tag(:div, value, class: "alert alert-#{key}")

      = form_tag administrator_logins_path do

        / .control-group
        /   = label_tag :username, "ユーザー名", class: 'control-label username-ttl'
        /   .controls
        /     = text_field_tag :username, params[:username], class: 'username'

        .control-group
          = label_tag :password, "パスワード", class: 'control-label password-ttl'
          .controls
            = password_field_tag :password, nil, class: 'password'

        .form-actions
          = submit_tag "ログイン", class: 'btn btn-primary'

        / %p
        /   = link_to "初めてご利用になる方はこちら", entry_signup_path, :target=>["_blank"]
        /   %br
        /   = link_to 'パスワードをお忘れの方はこちら', new_password_reset_path

      / %div.login-txt-02
      /   【 システム推奨環境 】 OS：Windows7  ブラウザ：IE9-10  プラグイン：Adobe Flash Player  その他：CookieおよびJavascriptをオンの状態でご利用ください。